{
  // Example: set Database:ConnectionString to point at any SQLite file.
  // You can replace the value below with the full path to the DB in your DataGrip project.
  // Example command-line override: --Database:ConnectionString="Data Source=/full/path/to/YourDb.db"
  "Database": {
    "ConnectionString": "Data Source=/path/to/your/datagrip/project/YourDb.db"
  }
}

